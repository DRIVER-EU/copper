{
  "name": "@layer-server/root",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tnocs/layer-server.git"
  },
  "scripts": {
    "postinstall": "lerna bootstrap && lerna run link",
    "dev:server": "concurrently --kill-others \"tsc -b -w packages\" \"nodemon --watch ./packages/server/dist packages/server/dist/index.js\"",
    "dev:client": "lerna run dev --parallel",
    "dev": "concurrently --kill-others \"tsc -b -w packages\" \"nodemon --watch ./packages/server/dist packages/server/dist/index.js\" \"yarn dev:client\"",
    "clean": "lerna run --parallel clean",
    "types": "./node_modules/.bin/typescript-json-schema.cmd packages/core/tsconfig.json ServerConfig *  -o ./schema/server.json",
    "build": "tsc -b packages",
    "linkserver": "lerna run linkserver",
    "start:client": "lerna run start:client --parallel",
    "start:server": "nodemon ./packages/server/dist packages/server/dist/index.js",
    "linkcs": "lerna run linkcs",
    "linkall": "yarn linkserver && yarn linkcs",
    "rebuild": "yarn clean && yarn && yarn linkall && yarn dev",
    "docker:build": "lerna run docker:build --parallel"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "lerna": "^3.13.2",
    "typescript-json-schema": "^0.37.0"
  },
  "nodemonConfig": {
    "ignore": [
      "configs/*"
    ],
    "delay": "2"
  },
  "dependencies": {
    "nodemon": "^1.18.11"
  }
}
